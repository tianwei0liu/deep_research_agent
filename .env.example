# Copy to .env and fill with real keys. Only sensitive values go here.
# Non-sensitive config (LangSmith project, tracing, etc.) is in settings.yaml.
LANGCHAIN_API_KEY=
DEEPSEEK_API_KEY=
GOOGLE_GEMINI_API_KEY=
TAVILY_API_KEY=
